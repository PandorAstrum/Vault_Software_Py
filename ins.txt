python -m PyInstaller --windowed --noconsole --clean --name Vault --icon data\res\icon\icon.ico data\main.py
python -m PyInstaller --onefile Vault.spec


pyinstaller.py --windowed --noconsole --clean --onefile maint.py

# -*- mode: python -*-
from kivy.deps import sdl2, glew
block_cipher = None


a = Analysis(['data\\main.py'],
             pathex=['C:\\Users\\Ana Ash\\Desktop\\Project Software\\KivyPy\\Vault'],
             binaries=[],
             datas=[],
             hiddenimports=[],
             hookspath=[],
             runtime_hooks=[],
             excludes=[],
             win_no_prefer_redirects=False,
             win_private_assemblies=False,
             cipher=block_cipher)
pyz = PYZ(a.pure, a.zipped_data,
             cipher=block_cipher)
exe = EXE(pyz,
          a.scripts,
          exclude_binaries=True,
          name='Vault',
          debug=False,
          strip=False,
          upx=True,
          console=False , icon='data\\res\\icon\\icon.ico')
coll = COLLECT(exe, Tree('data'),
               a.binaries,
               a.zipfiles,
               a.datas,
               *[Tree(p) for p in (sdl2.dep_bins + glew.dep_bins)],
               strip=False,
               upx=True,
               name='Vault')



designer//
@ change directory
@ python -m designer

git
git rm -r one-of-the-directories (delete folders)
